<!-- index.wxml -->
<view class="page">
  <view class="main">
  <view  wx:for="{{navigator}}" class="card1" wx:key="title">
    <navigator url="{{item.url}}" hover-class="none">
            <view class="card">
                <view class="card-img-wrapper">
                   <image src="{{item.png}}" class="card-img"/>
                </view>
                {{item.title}}
            </view>
        </navigator>
    </view>
    <view class="footer-desc">
    <text>本程序由乐橙云开放平台提供技术支持，更多信息请参考开发文档</text>
  </view>
  </view>
  <!-- <live-player id="live-player" src="{{src}}"></live-player>
  <live-pusher id="live-pusher" url="{{pushUrl}}" mode="RTC" 	enable-camera="{{false}}" style="width: 0px; height: 0px;"></live-pusher>
  <button bindtap="startTalk">开启对讲</button>
  <button bindtap="closeTalk">关闭对讲</button> -->
</view>